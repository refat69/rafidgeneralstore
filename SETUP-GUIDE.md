# 🚀 কীভাবে নিজের কম্পিউটারে চালাবো — Step by Step

## ধাপ ১: Node.js ইনস্টল
তোমার কম্পিউটারে **Node.js** লাগবে (ভার্সন 18 বা তার বেশি)।

👉 ডাউনলোড: https://nodejs.org → "LTS" ভার্সনটা ডাউনলোড করে ইনস্টল করো।
ইনস্টলের পর command prompt / terminal খুলে লিখে দেখো:
```
node -v
npm -v
```
উপরের মতো version নম্বর দেখালেই Node.js ঠিক আছে।

## ধাপ ২: প্রজেক্ট ফাইল ডাউনলোড ও আনজিপ
- `shop-dashboard.zip` ফাইলটা ডাউনলোড করো।
- ডেস্কটপে একটা ফোল্ডার বানাও (যেমন `Dokan`), zip-টা তার ভেতরে রেখে **এক্সট্র্যাক্ট (Extract)** করো।
- আনজিপের পর ভেতরে একটা `shop-dashboard` ফোল্ডার পাওয়া যাবে।

## ধাপ ৩: ফোল্ডারে ঢুকে ইনস্টল
Terminal/Command Prompt খুলে এই ফোল্ডারে যাও:
```
cd Desktop/Dokan/shop-dashboard
```
(তোমার যেখানে আছে সেই path দেবে)

তারপর ইনস্টল করো:
```
npm install
```
⚠️ প্রথমবার এটা কিছু সময় নেবে। এই ধাপে Next.js, MongoDB ইত্যাদি সব package যুক্ত হবে। শেষে MongoDB-র binary-ও ডাউনলোড হবে (৮০MB) — একটু সময় লাগবে।

## ধাপ ৪: চালাও (Run)
```
npm run dev
```
তারপর ব্রাউজারে খুলো: **http://localhost:3000**

## ধাপ ৫: লগইন
- **ইউজারনেম:** `admin`
- **পাসওয়ার্ড:** `admin123`

ব্যাপারটা শেষ! 🎉 শুরুতেই sample ডেটা (পণ্য, বিক্রি, বাকি, bKash, খরচ) নিজে থেকে ঢুকে যাবে।

---

## ❓ সাধারণ সমস্যা

**"MongoDB binary download" করে আটকে গেলে / error দিলে**
- Internet ভালো আছে কিনা দেখো।
- `npm install` এর পর আবার `npm run dev` চালাও।
- binary download-এর জন্য পুরনো Debian-এ সমস্যা হলে আমাদের দেওয়া newer version নিজে থেকেই নেবে।

**"Port 3000 already in use" বললে**
- হয় অন্য কিছু সেই পোর্ট ব্যবহার করছে। তাহলে:
```
npm run dev -- -p 3001
```
এবং ব্রাউজারে http://localhost:3001 খুলো।

**ডেটা হারিয়ে যাচ্ছে (প্রতিবার নতুন sample)**
- কারণ ডিফল্টে একটা **in-memory MongoDB** চলে — সার্ভার বন্ধ করলে ডেটা যায়।
- নিজের ডেটা **স্থায়ী** রাখতে MongoDB Atlas ব্যবহার করো (README-তে লিখা আছে)।

**bKash/বিক্রির পরিসংখ্যান কোথায়?**
- বিক্রি করলে স্টক কমে ও bKash লেজার আপডেট হয় — "Sell", "bKash", "Report" পেজে দেখবে।

## 💡 মনে রাখবে
- প্রতিবার চালাতে শুধু `npm run dev` আর `http://localhost:3000`।
- Real database লাগলে README-র "বাস্তব MongoDB (Atlas)" অংশ দেখো।
